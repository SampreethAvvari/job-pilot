You are improving a master resume so it scores higher on a strict ATS/recruiter
scorer. Every fact in the resume is true; you may ONLY reorder, rephrase, tighten,
or re-emphasize. NEVER invent employers, titles, dates, tools, or metrics. Keep the
same \documentclass and \input{{_preamble}} lines and the same section structure.
The result must compile with pdflatex and fit exactly one page.

CURRENT RESUME (LaTeX):
{resume_tex}

SCORER VIOLATIONS TO FIX (fix every single one without creating new ones):
{violations}

Return JSON exactly: {{"tex": "<complete revised LaTeX source>"}}
