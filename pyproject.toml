[project]
name = "jobpilot"
version = "0.1.0"
description = "Daily multi-source job search, scoring, dashboard, and digest pipeline"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "pydantic>=2.7",
    "pyyaml>=6.0",
    "google-api-python-client>=2.130",
    "google-auth>=2.30",
    "google-auth-oauthlib>=1.2",
    "google-genai>=1.0",
    "pypdf>=4.0",
    "google-cloud-secret-manager>=2.20",
]

[project.optional-dependencies]
dev = ["pytest>=8.2", "pytest-httpx>=0.30", "ruff>=0.4"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
jobpilot = ["prompts/*.txt", "resumes/*.tex"]

[tool.pytest.ini_options]
pythonpath = ["src", "."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
# Pin the rule set to ruff's historical default (E4/E7/E9/F). Without this,
# CI installs whatever ruff is newest and each release widens the default rule
# set, so unrelated pre-existing code starts failing lint on version drift.
select = ["E4", "E7", "E9", "F"]
